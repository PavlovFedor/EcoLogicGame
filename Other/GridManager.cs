using NUnit.Framework;
using UnityEngine;

public class GridManager : MonoBehaviour
{/*
    private const float tile_size = 1.0f;
    private const float tile_offset = 0.5f;

    private int selectionX = 1;
    private int selectionY = 1;

    public int tileX;
    public int tileY;
    private void Update()
    {
        DrawField();
    }
    private void DrawField()
    {
        Vector3 widthline = Vector3.right * tileX;
        Vector3 heightline = Vector3.forward * tileY;
        for (int i = 0; i <= tileX; i++)
        {
            Vector3 startpoint = Vector3.forward * i;
            Debug.DrawLine(startpoint, startpoint + widthline);
            for (int j = 0; j <= tileY; j++)
            {
                startpoint = Vector3.right * j;
                Debug.DrawLine(startpoint, startpoint + heightline);
            }
        }
    }
   */ 
}
